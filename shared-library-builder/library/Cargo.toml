[package]
name = "libgit2-library"
version = "1.6.5"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
shared-library-builder = { workspace = true, features = ["git-location", "cmake-library" ] }
libopenssl-library = { git = "https://github.com/feenkcom/libopenssl.git" }